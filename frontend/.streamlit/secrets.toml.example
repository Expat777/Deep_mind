# Пример секретов для Streamlit Community Cloud.
# На Streamlit Cloud задаётся через Settings → Secrets (НЕ коммитить реальный файл).
# Для локального теста скопируйте в frontend/.streamlit/secrets.toml.

API_URL = "http://ВАШ_VPS_IP:8000"
